<form class="d-inline" method="post" action="{{ path('app_admin_avatar_delete', {'id': avatar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avatar.id) }}">
    <button class="btn btn-outline-danger"><i class="fa fa-trash"></i> Supprimer</button>
</form>
