<div class="deznav">
	<div class="deznav-scroll">
		<div class="main-profile">
			<div class="image-bx">
				{% if userAvatar(app.user) %}
					<img src="{{ vich_uploader_asset(userAvatar(app.user)) | imagine_filter('medium_size') }}" class="rounded-circle" width="60" height="60" alt="{{ app.user.pseudo }}"/>
				{% else %}
					<img src="{{ asset('img/user.jpg') }}" width="60" height="60" class="rounded-circle img-1">
				{% endif %}
			</div>
			<h5 class="name">
				<span class="font-w400">Salut,</span>
			{{ app.user.pseudo }}</h5>
			<p class="email">{{ app.user.email }}</p>
		</div>
		<ul class="metismenu" id="menu">
			<li class="nav-label first">Menu principal</li>
			<li>
				<a class="" href="{{ path('app_admin_dashboard') }}" aria-expanded="false">
					<i class="flaticon-144-layout"></i>
					<span class="nav-text">Tableau de bord</span>
				</a>
			</li>
			<li class="nav-label">Contenu</li>
			<li>
				<a class="has-arrow ai-icon" href="javascript:void()" aria-expanded="false">
					<i class="flaticon-077-menu-1"></i>
					<span class="nav-text">Utilisateurs</span>
				</a>
				<ul aria-expanded="false">
					<li>
						<a href="{{ path('app_admin_users_crud_index') }}">Tout les utilisateur</a>
					</li>
					<li>
						<a href="{{ path('app_admin_avatar_index') }}">Avatars de comptes</a>
					</li>
					<li>
						<a href="{{ path('app_admin_users_crud_new') }}">Créer un utilisateur</a>
					</li>
				</ul>
			</li>
			<li>
				<a class="" href="{{ path('app_admin_missions_crud_index') }}" aria-expanded="false">
					<i class="flaticon-077-menu-1"></i>
					<span class="nav-text">Missions</span>
				</a>
			</li>
			<li>
				<a class="" href="{{ path('app_admin_paiements_crud_index') }}" aria-expanded="false">
					<i class="flaticon-077-menu-1"></i>
					<span class="nav-text">Demandes de paiements</span>
				</a>
			</li>
		</ul>
		<div class="copyright">
			<p>
				<strong>{{ appName }} Admin Dashboard</strong>
			© 2021 All Rights Reserved</p>
			<p class="fs-12">Made with love
				<span class="heart"></span>
			by {{ appConcepteur }}</p>
		</div>
	</div>
</div>

